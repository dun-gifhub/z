# Server Port (default 3000)
PORT=3000

# Admin panel gate password
ADMIN_PASSWORD=

# Database URL (optional PostgreSQL/Neon connection string, defaults to in-memory store if omitted)
DATABASE_URL=

# Environment
NODE_ENV=development
